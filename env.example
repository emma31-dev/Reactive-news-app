# Reactive Network Smart Contract Configuration
# Replace with your actual deployed contract address
NEXT_PUBLIC_REACTIVE_NEWS_CONTRACT=0x0000000000000000000000000000000000000000

# Reactive Network Configuration
NEXT_PUBLIC_REACTIVE_NETWORK_RPC=https://sepolia-rpc.reactive.network
NEXT_PUBLIC_REACTIVE_NETWORK_EXPLORER=https://sepolia-explorer.reactive.network
NEXT_PUBLIC_REACTIVE_CHAIN_ID=5318008

# SMTP / Email (configure to enable real verification emails)
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM=Reactive News <no-reply@yourdomain.com>

# Development fallback: if outbound SMTP (Ethereal or real) is blocked (ETIMEDOUT),
# and you still want to test the UI flow, you can enable a mock delivery that
# generates a preview URL without sending. DO NOT enable in production.
# ALLOW_DEV_EMAIL_MOCK=true